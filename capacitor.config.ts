import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.charanteja.learningloop.app',
  appName: 'learningloop',
  webDir: 'dist'
};

export default config;
